# Update these with your Supabase details from your project settings > API
# https://app.supabase.com/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=your-project-url
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_OR_ANON_KEY=your-anon-key

# Service role key for server-side operations that bypass RLS (cron jobs, admin operations)
# WARNING: This key bypasses all security policies - keep it secret!
# Get this from Supabase dashboard > Project Settings > API > service_role key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Cron job security - Generate a random string of at least 16 characters
# Example: Use a password generator like 1Password or: openssl rand -hex 16
CRON_SECRET=your-cron-secret-here
